import { classnames } from 'tailwindcss-classnames'

export const listWrapper = classnames('flex', 'flex-col', 'gap-8')

export const keyResultWrapper = classnames('flex', 'flex-col', 'gap-4')

export default {}
